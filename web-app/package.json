{
  "name": "protube-web-app",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TV app (Tizen / webOS / Android TV WebView) — biên dịch từ src/app.ts ra js/app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
